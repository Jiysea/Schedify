@model ViewResourceViewModel

<div x-cloak x-show="viewResourceModal" x-transition.opacity
    class="fixed inset-0 flex justify-center items-start bg-black/50 backdrop-blur-sm overflow-y-auto scrollbar-thin scrollbar-track-transparent scrollbar-thumb-coffee-700 z-50 p-4">

    @* Box *@
    <div id="viewResourceModalContent" x-data x-trap="viewResourceModal" class="bg-coffee-50 rounded-lg shadow-lg md:min-w-[45rem] max-w-[45rem]"
        @@keydown.escape = "viewResourceModal = false;">

        @* The Partials will be swapped here *@

    </div>
</div>